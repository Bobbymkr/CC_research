## Graphify Usage Rules
- Before answering architecture questions, check `graphify-out/GRAPH_REPORT.md`.
- For "how does X relate to Y" questions, use `graphify path "X" "Y"` or `graphify query`.
- After significant code changes, run `graphify update .` to keep the graph current.
