# RAASA AWS Phase 1 Progress Tracker

Important update:

- This ML-centric tracker is preserved as historical execution context.
- After direct inspection of the restarted live instance (`54.227.40.170`) on April 26, 2026, the primary Phase 1 direction is now better captured in `AWS_Phase1_Development_Direction_2026_04_26.md`.
- The reason for that pivot is simple: current live evidence shows controller-correctness and enforcement-scope issues that are more important to solve than pushing the ML story further first.

## Current status after Phase 1D

- The strongest current AWS/K8s result is now the Phase 1D universal pod-resolution validation, not the earlier ML enablement milestone.
- The clearest paper-safe cloud-native claim is now: RAASA can perform pod-specific host-veth containment across the tested live AWS demo and benchmark pods, while the control-plane telemetry path remains operationally fragile under stress.
- The tuned linear controller remains the truthful primary controller for the paper story; the Isolation Forest path is still secondary until a new ablation proves otherwise.
- The latest controller-calibration loop now shows a stronger live controller story than before: `default/raasa-test-benign-compute` can be held at `L1` while `default/raasa-test-malicious-cpu` still escalates to `L3`, but only after live K8s probe-path and confidence-floor calibration.
- The remaining primary AWS/K8s blocker is now Metrics API instability and control-plane timeout behavior, not the basic enforcement or controller-threshold story.

## Immediate next actions

1. Stabilize the K8s Metrics API and control-plane telemetry path, because current live controller iterations are still stretched by repeated `metrics.k8s.io` timeout failures.
2. Preserve the calibrated live controller settings now validated in `phase1g4b_calibration_snapshot_2026_04_26/`: `l2_max=0.60`, `l3_min_confidence=0.05`, `syscall_cap=5000.0`, and probe path `/var/run/raasa`.
3. Re-run the refined Phase 1F containment validation after the next telemetry/control-plane fix to confirm the calibrated controller work has not disturbed the pod-specific `L3` containment path.
4. Keep the paper centered on adaptive-vs-static trade-offs, tuned linear control, and pod-specific containment evidence.
5. Treat richer ML claims, seccomp/CRIU language, and stronger telemetry guarantees as future-facing unless new evidence is collected.

## Objectives for Phase 1
1. **Model Training:** Extract baseline telemetry (`audit.jsonl`) from Phase 0 and train an Isolation Forest ML model.
2. **Model Deployment:** Deploy the `.joblib` ML model into the `raasa-agent` via a Kubernetes ConfigMap or Volume.
3. **Configuration Update:** Update the agent's `config.yaml` to enable `use_ml_model: true` and point to the model path.
4. **Enforcement Validation:** Observe the ML model detecting the 15% CPU `stress-ng` workload (malicious pod) as an anomaly.
5. **Traffic Control (tc) execution:** Verify that the `raasa-enforcer` sidecar successfully receives the IPC command and executes network isolation/cgroup throttling on the anomalous pod.

## Activity Log

- **[2026-04-26] Setup Progress Tracker:** Created this document to record Phase 1 execution steps.
- **[2026-04-26] Extracting Baseline Data:** Successfully extracted `run_20260426T043009Z.jsonl` from the `raasa-agent` pod to build the ML training dataset.
- **[2026-04-26] Model Training:** Developed and ran a Python script locally to parse the JSONL telemetry logs. Trained a `scikit-learn` Isolation Forest model with 10% contamination rate on 320 valid baseline telemetry records, saving to `model.joblib`.
- **[2026-04-26] Model Deployment & Config Update:** Uploaded `model.joblib` to the AWS instance. Rebuilt the `raasa/agent:1.0.0` Docker image to include the model. Updated `k8s/daemonset.yaml` to set `use_ml_model: true` and pointing to `/app/raasa/models/model.joblib`, then applied via `kubectl rollout restart`.
- **[2026-04-26] Controller Truthfulness Issue Found:** Direct live inspection on the restarted AWS node showed that the running ConfigMap still had `use_ml_model: true` while audit records claimed `controller_variant: linear_tuned`. This made the Phase 1 state experimentally misleading.
- **[2026-04-26] Pre-Ablation State Preserved:** Saved the live pre-change state into `phase1_ablation_ml_vs_linear_2026_04_26/before_configmap_ml_on.yaml` and `phase1_ablation_ml_vs_linear_2026_04_26/before_audit_ml_on.jsonl`.
- **[2026-04-26] Live Linear Baseline Applied:** Updated the running AWS K8s ConfigMap to `use_ml_model: false`, restarted the RAASA DaemonSet, and preserved the truthful post-change state in `phase1_ablation_ml_vs_linear_2026_04_26/after_configmap_linear.yaml`.
- **[2026-04-26] Phase 1 Ablation Outcome:** The truthful linear baseline preserved `L3` for `raasa-test-malicious-cpu`, reduced `raasa-net-client` from `L2` to `L1`, and showed that `raasa-test-benign-compute` is now better understood as a workload/de-escalation tuning issue rather than a pure ML problem. See `AWS_Phase1_Ablation_Report_2026_04_26.md`.
- **[2026-04-26] Phase 1B Blast-Radius Benchmark Added:** Added `k8s/phase1b-network-benchmark.yaml` and `scripts/run_phase1b_blast_radius.ps1` to test live AWS network-enforcement behavior through the same `raasa-enforcer` path used by the prototype.
- **[2026-04-26] Phase 1B Enforcement Finding:** Live AWS testing showed that `tc` state on `cni0` changes correctly, but same-node pod-to-pod benchmark traffic does not slow down in a way consistent with a real `1mbit` cap. The canonical refined evidence bundle is `phase1b_blast_radius_2026_04_26_refined/`.
- **[2026-04-26] Phase 1B Calibration Finding:** Quantified `benign-compute` as a syscall/de-escalation tuning issue rather than a general controller failure. See `AWS_Phase1B_Blast_Radius_Report_2026_04_26.md` and `phase1b_blast_radius_2026_04_26_refined/benign_compute_calibration_summary.json`.
- **[2026-04-26] Phase 1C Pod-Specific Enforcer Implemented:** Updated the privileged enforcer to resolve pod UIDs to host-side veth interfaces using the in-cluster API plus `nsenter -n -m`, and updated the agent image to include `util-linux` for `nsenter`.
- **[2026-04-26] Phase 1C Live Deployment Succeeded:** Deployed `raasa/agent:phase1c3` to the AWS node and confirmed the DaemonSet rolled to a new live pod. Canonical deployment evidence: `phase1c3_deploy_2026_04_26/`.
- **[2026-04-26] Phase 1C Containment Breakthrough:** The final live benchmark run showed `raasa-bench-client` resolving to `veth04a1b488`, with `L1` averaging `0.037 s` and `L3` collapsing to roughly `122.7 s` and `0 B/s`, plus connection resets. Canonical validation evidence: `phase1c3_pod_specific_validation_2026_04_26_complete/`.
- **[2026-04-26] Phase 1D Root Cause Identified:** Direct live inspection showed the remaining `raasa-net-server` failure was caused by a resolver dependence on `sh` inside the target mount namespace; the `hashicorp/http-echo` image did not provide that shell.
- **[2026-04-26] Phase 1D First Correction Rejected:** A shell-free `/sys/class/net/.../iflink` attempt under only the target network namespace incorrectly mapped workloads to `lo`, so that approach was rejected rather than papered over.
- **[2026-04-26] Phase 1D Universal Resolution Fix Applied:** Updated the enforcer to parse peer indices directly from `nsenter -n ip -o link show` output like `eth0@if6`, then map those peer ifindices back to host-side veth interfaces.
- **[2026-04-26] Phase 1D Final Live Deployment Succeeded:** Deployed the corrected `raasa/agent:phase1d` image and rolled the DaemonSet to `raasa-agent-4hq5z`. Canonical deployment evidence: `phase1d2_deploy_2026_04_26/`.
- **[2026-04-26] Phase 1D Universal Resolution Validated:** The canonical validation bundle `phase1d2_resolution_validation_2026_04_26/` showed clean resolution for `raasa-net-server`, `raasa-net-client`, and `raasa-bench-client`, with `fallback_lines = []`, while preserving the benchmark containment result (`L1` about `0.014 s`, `L3` about `123.05 s`, `0 B/s`). See `AWS_Phase1D_Universal_Resolution_Report_2026_04_26.md`.
- **[2026-04-26] Post-Phase-1D Source Hardening:** Local review of `raasa/k8s/enforcer_sidecar.py` found that the new `NETWORK_PROFILE_BY_TIER` table had been introduced while `_apply_network_throttle()` still referenced stale undefined names from the older TBF-only implementation. The source was corrected to use the declared tier profiles (`L1` clear, `L2` degraded `tbf`, `L3` hard `netem loss`), mirrored into `RM_practical/code/k8s/enforcer_sidecar.py`, and covered by `tests/test_enforcer_sidecar.py`.
- **[2026-04-26] Local Verification Note:** The focused `unittest` coverage for `tests/test_enforcer_sidecar.py` now passes in the bundled runtime. Broader suite execution in that runtime still requires missing packages such as `PyYAML`, `joblib`, and `pytest`, so the next AWS loop should treat the enforcer unit test as the immediate regression guard and the live Phase 1D rerun as the next end-to-end check.
- **[2026-04-26] Phase 1E Telemetry Correctness Hardening Started:** The K8s observer had regressed into a probe-only CPU path, which caused mocked and degraded Metrics API scenarios to report `0.0%` CPU even when CPU usage strings were present. The observer was corrected so direct `.cpu_usec` probe data remains the preferred source while Metrics API CPU parsing is restored as the fallback path. Focused regression coverage now passes for both `tests/test_k8s_observer.py` and `tests/test_enforcer_sidecar.py`, and the curated packet copies were synchronized in `RM_practical/code/k8s/observer_k8s.py` and `RM_practical/verification/tests/test_k8s_observer.py`.
- **[2026-04-26] Phase 1E Live Deployment Succeeded:** Built `raasa/agent:phase1e`, deployed it to the AWS K3s node, and observed a clean DaemonSet rollout to `raasa-agent-nlzh9`. Deployment evidence: `phase1e_deploy_2026_04_26/`.
- **[2026-04-26] Phase 1E Live Resolution Validation Succeeded With Changed L3 Semantics:** The rerun in `phase1e_resolution_validation_2026_04_26/` preserved clean pod resolution for `raasa-net-server`, `raasa-net-client`, and `raasa-bench-client`, again with `fallback_lines = []`. `L1` stayed fast (`avg_time_total_sec` about `0.0090 s`), while `L3` now failed fast at about `4.96 s` with `0 B/s`, rather than the older `~123 s` stall pattern. This indicates stronger hard containment semantics in the live path, likely including service-resolution loss under `L3`, and should be described as containment rather than throughput shaping.
- **[2026-04-26] Phase 1F Validation Refinement Implemented:** Updated `raasa/scripts/run_phase1d_resolution_validation.ps1` so the benchmark path now tests three targets in both `L1` and `L3`: the Kubernetes service DNS name, the service `ClusterIP`, and the benchmark server pod IP. The script also captures DNS diagnostics from inside the benchmark client pod. The curated packet copy was synchronized in `RM_practical/code/k8s/run_phase1d_resolution_validation.ps1`.
- **[2026-04-26] Phase 1F Live Semantics Clarified:** The refined run in `phase1f_resolution_validation_2026_04_26/` preserved clean pod-specific host-veth resolution with `fallback_lines = []`, while showing a stronger result than before: under `L1`, service DNS, `ClusterIP`, and direct pod IP all work normally; under `L3`, DNS resolution fails (`nslookup` timeout and empty `getent`), service `ClusterIP` traffic fails, and direct pod IP traffic also fails with `0 B/s`. This means the current `L3` semantics should be described as hard containment that disrupts both name resolution and direct network reachability, not as bandwidth shaping.
- **[2026-04-26] Phase 1G Calibration Infrastructure Added:** Added `raasa/scripts/apply_raasa_config_to_aws.ps1` so live controller config can be rolled onto the AWS K3s node without accidentally resetting the running agent image tag, and added `raasa/scripts/collect_phase1g_calibration_snapshot.ps1` so live controller status/audit snapshots can be collected repeatably.
- **[2026-04-26] Phase 1G First Calibration Attempt Exposed Probe-Path Drift:** The first live calibration apply showed that the checked-in probe config still pointed `syscall_probe_directory` at `raasa/runtime/syscalls`, while the actual live K8s shared probe volume is `/var/run/raasa`. This muted the live syscall signal and temporarily forced the controller into an invalid comparison state. The probe config was corrected before accepting any controller conclusion.
- **[2026-04-26] Phase 1G Live Controller Calibration Succeeded:** The corrected live calibration path now uses `l2_max=0.60`, `l3_min_confidence=0.05`, `syscall_cap=5000.0`, `use_ml_model=false`, and probe path `/var/run/raasa`. In the canonical snapshot `phase1g4b_calibration_snapshot_2026_04_26/`, `default/raasa-test-benign-compute` remains in `L1` (`risk≈0.115`), while `default/raasa-test-malicious-cpu` moves `L1 -> L2 -> L3` (`risk=1.000`, `confidence≈0.062`). This is the strongest truthful live controller-calibration result so far.
- **[2026-04-26] Phase 1G Residual Operational Limitation:** Even after the controller-calibration fix, the agent stdout still shows repeated `metrics.k8s.io` `500` errors caused by control-plane `subjectaccessreviews` timeouts. That issue now defines the next technical phase more than controller-threshold tuning does.
